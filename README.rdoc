== Dare Kickstarter

Instructions for deployment. Run in Ruby 2.1.3.

1. Run `bundle install` to install all Ruby gems.
1. Copy and paste the `secrets.yml.example` to a `secrets.yml` file.

Good luck!